/**
 * Идентифицированный через Bearer-JWT пользователь, от имени которого выполняется
 * MCP-запрос к `/mcp`. Формируется `McpAuthGuard` из payload токена (поле `sub`) и
 * пробрасывается по цепочке McpController → McpService → регистраторы инструментов,
 * которые используют его для авторизации (владение встречей/задачей).
 */
export interface Requester {
  id: string;
}
